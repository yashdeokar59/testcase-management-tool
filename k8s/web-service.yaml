apiVersion: v1
kind: Service
metadata:
  name: web-service
  namespace: test-management
  labels:
    app: test-management-tool
    component: web
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 5000
    nodePort: 30080
    protocol: TCP
    name: http
  selector:
    app: test-management-tool
    component: web
