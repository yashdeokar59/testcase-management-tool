apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: test-management-ingress
  namespace: test-management
  labels:
    app: test-management-tool
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.ingress.kubernetes.io/ssl-redirect: "false"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "false"
spec:
  ingressClassName: nginx
  rules:
  - host: test-management.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: web-service
            port:
              number: 80
  # Uncomment for HTTPS
  # tls:
  # - hosts:
  #   - test-management.local
  #   secretName: test-management-tls
